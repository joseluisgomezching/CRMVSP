# Server secret. Never commit the actual private key.
GOOGLE_SERVICE_ACCOUNT_JSON='{"client_email":"...","private_key":"..."}'
# Or use Application Default Credentials on the server.
GEMINI_API_KEY=""
VITE_PUBLIC_APP_URL=""

# Administrator access. Generate the password hash with:
# node scripts/hash-password.cjs
ADMIN_PASSWORD_HASH=""
# Generate with: node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=""

# Alternative for folders in a person's My Drive: owner OAuth refresh token,
# created once with offline consent for Drive and Sheets scopes. Server only.
GOOGLE_OAUTH_CLIENT_ID=""
GOOGLE_OAUTH_CLIENT_SECRET=""
GOOGLE_OAUTH_REFRESH_TOKEN=""
